# Qwen3-VL-8B-Instruct zero-shot baseline on VisFinEval

> 运行时间: 2026-09-11 03:24 – 03:59 (35 min)
> 主机: user-SYS-420GP-TNR · GPU 4-7 (4×RTX4090)
> 模型: `Qwen/Qwen3-VL-8B-Instruct` @ bf16
> 环境: torch 2.6.0+cu124 / transformers 5.16.1 / ms-swift 4.6.0.dev0 (0673cf75)
> 解码: greedy, max_new_tokens=16
> 原始预测: `results/zeroshot/predictions.shard{0..3}.jsonl` (19,208 条)

## 1. 主结果

```
                         n         raw      常数基线      macro-recall
  多选题 · 全部      16,404     73.82%      58.54%        71.03%
  多选题 · 单图      14,965     74.53%      59.90%        72.32%
  多选题 · 多图       1,439     66.44%      44.41%        65.53%
  判断题 · 全部       2,804     77.46%      73.57%        66.26%
  ─────────────────────────────────────────────────────────────────
  总体               19,208     74.35%      49.99%        69.44%
```

「常数基线」= 永远输出该题型的多数类答案（多选题选 A / 判断题选「是」）。
**这是及格线，不是零分线。**

## 2. 发现一：raw accuracy 掩盖了判断题的少数类崩溃

判断题看起来最好（raw 77.46%），但拆开看：

```
  真值     n     占比     召回率
  是    2,063   73.6%    90.0%
  否      741   26.4%    42.5%   ← 低于随机猜 (50%)
```

- raw 77.46% vs macro-recall 66.26%，**差 11.20 个百分点**（全数据集中最大）
- 模型在少数类「否」上的召回率 **42.5%，比抛硬币还差**
- 预测分布 是:81.4% / 否:18.5% —— 比真值分布(73.6/26.4)更极端

**结论**：模型在判断题上接近退化为「全选是」，但又不是完全退化
（它答了 520 次「否」，只对 315 次）。

## 3. 发现二：多图题存在清晰的崩溃曲线

```
   图数     n       raw      常数基线      增益
    2     666     75.5%      45.8%      +29.7pp
    3     380     62.4%      44.2%      +18.2pp
    4     227     61.7%      29.5%      +32.2pp
    5      23     87.0%      78.3%       +8.7pp
    6       4     75.0%      50.0%      +25.0pp
    8      60     50.0%      56.7%       -6.7pp   ← 低于基线
   10      78     29.5%      59.0%      -29.5pp   ← 崩溃
   11       1      0.0%     100.0%     -100.0pp
```

**10 张图的题：raw 29.5%，比「无脑全选 A」低 29.5 个百分点**（n=78）。

图数 ≥8 时性能崩塌，说明模型的跨图表整合能力存在明确上限。
这是金融分析的真实场景（对比多期财报），值得深入。

## 4. 发现三：模型的答案偏差方向不一致

```
多选题  多数类「A」 真值 58.5% -> 模型 48.1%   (-10.4pp, 逆着偏差走)
判断题  多数类「是」真值 73.6% -> 模型 81.4%   ( +7.8pp, 顺着偏差走)
```

同一个模型，在两种题型上朝相反方向偏离数据分布。

一个待验证的假设：多选题上模型被 RLHF 训练出「不要总选同一项」的倾向，
而判断题上「是」的语言先验压过了这个倾向。**需要额外实验才能确认，此处仅记录现象。**

## 5. 发现四：数据集本身存在标注缺陷

模型输出多个字母（`ABCD` / `ABC` / `AB`）共 55 条，全部被现有提取规则判为错误。
逐条检查发现：

- 这 55 条**全部**是「以下**哪些**……」句式（例：*以下哪些机构直接或间接持有…的股份？*）
- 「哪些」在这批中占 87%，而全量数据中只占 8.10% —— **富集 10.8 倍**
- 但 gold 只标注了单个字母（如 `A`）

**这些不是模型的错误，是标注格式与问题语义不匹配。**
多选题问「哪些」却只有单选答案，本身是矛盾的。

占比 0.33%，量不大，但属于系统性缺陷。

## 6. 复现方式

```bash
cd VLM
bash scripts/run_eval.sh                      # 4 卡全量，约 35 分钟
python scripts/analyze_results.py --dir results/zeroshot
```

## 7. 附加实验：prompt 敏感性验证

> 目的：确认发现一（判断题少数类召回率 42.5%）是模型性质，还是单一 prompt 的假象。
> 运行：2026-09-11 12:06–12:37 · 4×RTX4090 · 6 个变体 · 约 17k 次推理
> 原始预测：`results/sensitivity/{tf,mc}_{base,nobias,correct_wrong,ab_format}/`

### 7.1 判断题（n=2804 × 4 变体）

```
变体                                     raw      常数基线    macro     是召回    否召回
base (baseline 措辞)                  77.5%     73.6%     66.3%    90.0%    42.5%
nobias (显式提示不要默认选「是」)          76.6%     73.6%     67.9%    86.5%    49.3%
correct_wrong (用「正确/错误」)         76.5%     73.6%     73.0%    80.4%    65.6%
ab_format (转成 A/B 选项)              77.5%     73.6%     72.3%    83.3%    61.3%
```

逐样本配对比较（相对 base，同一 uid）：

```
变体                    翻对    翻错    净变化
ab_format              175    175      +0
correct_wrong          179    206     -27
nobias                  55     78     -23
```

**结论 A —— 少数类崩溃是模型性质，不是 prompt 假象。**
四个变体下「否」召回率全部显著低于「是」（差 22~48 个百分点）。

**结论 B —— 但 prompt 的影响大到足以推翻结论。**
仅把「是/否」换成「正确/错误」，少数类召回率就从 **42.5% 提升到 65.6%**，
macro-recall 从 66.3% → 73.0%。**换个词就暴露出 6.7 个百分点的真实能力差异。**

**结论 C —— raw accuracy 与 macro-recall 方向相反。**
所有变体的 raw 都不优于 base（净变化 −27 / −23 / 0），但 macro-recall 提升最多 6.7pp。
**只按 raw accuracy 调 prompt，会选出少数类召回率最低的那个版本。**

**结论 D —— `nobias` 失效。**
显式告诉模型「正确答案不一定是是，别默认选是」，净变化 **−23，反而变差**。
说明这不是「模型不知道要平衡」，而是**它做不到**。

### 7.2 多选题（n=3000 × 2 变体）

```
变体              raw      常数基线    macro     A       B       C       D
base            73.4%     59.8%     70.5%   73.0%   77.9%   68.7%   62.4%
nobias          72.1%     59.8%     70.1%   70.2%   79.5%   69.3%   61.5%
```

换措辞只影响 1.3pp。

**结论 E —— 题型对 prompt 的敏感度差异巨大。**
多选题对措辞不敏感（<1.5pp），判断题极度敏感（macro 差 6.7pp）。
**这意味着两类题型的评测可信度不同：判断题的结论必须固定 prompt 并同时报告 macro。**

## 8. 附加实验：LoRA 微调能否修复少数类崩溃？

> 运行：2026-09-11 · 4×RTX4090
> 基座 `Qwen3-VL-8B-Instruct`，LoRA r=16 α=32，`freeze_vit=true`，2 epoch
> 评测：同一批 **2,889 条 test 样本**（按来源研报切分，与训练集零重叠）
> 原始预测：`results/posttrain_{zeroshot,lora_natural,lora_balanced}/`

### 8.1 主结果

```
条件                      多选raw    判断raw   判断macro   「否」召回 [95% Wilson CI]
zero-shot (基座)         73.70%    75.23%    63.73%    39.7% [31.2, 48.8]
LoRA natural            84.13%    84.01%    79.15%    69.0% [60.1, 76.7]
LoRA balanced           82.78%    84.01%    79.43%    69.8% [60.9, 77.4]
```

**「否」召回率的两个置信区间完全不重叠**（[31.2, 48.8] vs [60.1, 76.7]）。
微调把 zero-shot 上「低于随机猜」的少数类召回率拉到了 69%。

### 8.2 逐样本配对比较（同一 uid）

```
对比                              翻对   翻错    净变化   McNemar
zero-shot -> LoRA natural         430    136    +294    p < 0.001 ***
zero-shot -> LoRA balanced        428    167    +261    p < 0.001 ***
LoRA natural -> LoRA balanced      84    117     -33    p = 0.024 *
```

在「否」子集（n=116）上：

```
zero-shot -> LoRA natural          40      6     +34
zero-shot -> LoRA balanced         44      9     +35
LoRA natural -> LoRA balanced       9      8      +1
```

### 8.3 关键发现：增益几乎全部落在少数类上

```
                     zero-shot -> LoRA natural
「是」召回 (n=328)      87.8%  ->  89.3%     +1.5pp
「否」召回 (n=116)      39.7%  ->  69.0%    +29.3pp
```

整体净提升 +294/2889 = +10.2pp，而少数类提升 +29.3pp。
**微调不是在均匀地提升所有题，而是专门修复了模型忽略少数类的问题。**

### 8.4 负结果：类别均衡采样没有帮助

`balanced` 条件把训练集的 6 个（题型 × 标签）组合各上采样到 6,725 条，
完全均衡。结果：

- 多选 raw：natural 84.13% vs balanced **82.78%** —— balanced 反而**低 1.35pp**（p=0.024）
- 判断题「否」召回：69.0% vs 69.8% —— 差异在噪声内（配对净变化 +1）

**结论：少数类崩溃不是「训练数据标签不平衡」造成的。**
普通的、按原始分布训练的 SFT 就能修复它，不需要重采样。
均衡采样不仅没帮上忙，还略微损害了多选题的准确率。

一个可能的机制：微调让模型学会了「读图答题」这个任务本身，
从而降低了对外部语言先验的依赖 —— 少数类召回率的提升是这件事的副产品，
而不是靠喂更多少数类样本换来的。**此机制尚未被直接验证，仅为推测。**

## 9. 附加实验：多图崩溃是 token 预算问题吗？

> 运行：2026-09-11 · 4×RTX4090 · 2,939 条 × 4 档预算
> 原始预测：`results/tokenbudget_{nocaq,cap1m,cap500k,cap250k}/`

### 9.1 背景：一个被忽略的配置事实

Qwen3-VL 的 processor 默认 `longest_edge = 16777216`（1600 万像素），**等于没有上限**。
实测（`apply_chat_template` 后的图像 pad 数）：

```
单图视觉 token 数（实测）
  原图（最大 5.8M 像素）    4281
  longest_edge = 1.0M       979     (压缩 4.4x)
  longest_edge = 0.5M       461     (压缩 9.3x)
  longest_edge = 0.25M      243     (压缩 17.6x)
```

⚠ **`MAX_PIXELS` 环境变量是 ms-swift 的约定，transformers 的 processor 不读它。**
想控制分辨率必须直接改 `processor.image_processor.size`。
本仓库此前的评测用的是原图分辨率，而训练用了 `MAX_PIXELS=1003520` ——
**存在训练/评测口径不一致**，本实验同时量化了它的影响。

### 9.2 主结果

```
               单图tok    1图     2图    3-4图   5-6图   8图+    全部
无上限           4281   74.7%  75.5%  62.1%  85.2%  38.8%   70.7%
cap 1.0M         979   74.5%  74.8%  59.6%  70.4%  43.2%   70.0%
cap 0.5M         461   72.5%  74.9%  56.3%  66.7%  43.9%   68.3%
cap 0.25M        243   70.1%  69.7%  52.7%  59.3%  35.3%   64.6%

样本量                  1500    666    607     27    139
```

### 9.3 结论一：多图崩溃**不是** token 预算造成的

假设「token 预算耗尽导致 8+ 图崩溃」被以下证据否定：

1. **「8图+」档收紧预算后反而变好**：38.8% → 43.2% → 43.9%。
   只有压到 243 token 才掉。该档显然不受预算限制。
2. **跌幅量级相当**：单图 −4.7pp（n=1500）vs 多图 −7.5pp（n=1439 加权），
   差 −2.8pp，在噪声量级内。预算压缩是**普遍性伤害**，不是多图特有。
3. **曲线形状不随预算变化**：相对同预算下的「1图」，
   「8图+」在 4 档下分别是 −35.9 / −31.4 / −28.6 / −34.8 pp —— 稳定。

→ **8+ 图崩溃是模型的能力上限，需要改架构或训练目标，不是调分辨率能解决的。**

> ⚠ 一个统计教训：5-6 图档只有 27 条，其 −25.9pp 的跌幅会把**未加权**的
> 平均带偏，得出「支持假设 A」的错误结论。必须按样本量加权。

### 9.4 结论二（实用）：1M 是安全的工作点

```
cap        单图token    整体准确率    相对无上限
无上限        4281       70.7%         —
cap 1.0M       979       70.0%       -0.7pp    ← 省 4.4x token，几乎无损
cap 0.5M       461       68.3%       -2.4pp
cap 0.25M      243       64.6%       -6.1pp    ← 开始有明显代价
```

**建议：多图场景用 `longest_edge = 1003520`。** 视觉 token 省 4.4 倍、
精度损失 < 1pp。低于 0.5M 后代价迅速上升。

这也解释了此前的训练/评测不一致为何没有致命影响 ——
训练在 1M 口径、评测在原图口径，两者的差距约 0.7pp。

## 10. 附加实验：跨代对比 —— 新一代模型反而更不可靠

> 运行：2026-09-12 · 4×RTX4090
> 同一测试集（2,889 条）、同一 prompt（base）、同一指标口径
> 原始预测：`results/posttrain_zeroshot/`（Qwen3-VL-8B）、`results/model_qwen25vl-7b/`（Qwen2.5-VL-7B）

### 10.1 动机

前九个实验都在 Qwen3-VL-8B 一个模型上做。一个自然的质疑是：
**「少数类崩溃是 VisFinEval 这个 benchmark 的问题，还是这个模型的问题？」**

要区分这两者，只需要在同家族里换一代模型，其余全部不变。

### 10.2 结果

```
模型                        多选raw  常数基线  判断raw  判断macro  「是」召回  「否」召回 [95% CI]
Qwen2.5-VL-7B（上一代）      69.7%   58.6%   75.7%    74.3%     77.1%   71.6% [62.8, 79.0]
Qwen3-VL-8B （新一代）       73.7%   58.6%   75.2%    63.7%     87.8%   39.7% [31.2, 48.8]
```

差　异　　　　　　　　　   **+4.0pp**　　　  　-0.5pp　**-10.6pp**　+10.7pp　**-31.9pp**

### 10.3 三个结论

**① 新一代在「平均」上更好，在「长尾」上更差。**
Qwen3-VL 的多选 raw 提升 4.0pp，同时少数类召回率崩塌 31.9pp。
两个置信区间完全不重叠（[31.2, 48.8] vs [62.8, 79.0]）。

**② 这排除了「benchmark 坏了」的解释。**
同一个 benchmark、同一个切分、同一个 prompt，在上一代模型上给出健康的 71.6%。
失败属于模型本身，不是数据集的缺陷。

**③ 只报 raw accuracy 的榜单看不到这件事。**
它会因为 +4.0pp 把 Qwen3-VL 排在上一代之上，而完全掩盖 −31.9pp 的回归。
**这是本项目核心论点最干净的一次实证。**

### 10.4 一个反差值得注意

Qwen2.5-VL 在**多数类上更差**（「是」召回 77.1% vs 87.8%），
但换来了远为均衡的 macro 表现（74.3% vs 63.7%）。

也就是说：**两代模型在「倾向多数类」这件事上的程度不同**，
而 raw accuracy 恰好把这个差异完全抹平了（75.7% vs 75.2%，看起来几乎一样）。

### 10.5 局限

- 只对比了两代同家族模型。跨家族（InternVL 等）尚未纳入，不能断言这是所有新一代 VLM 的通病
- 两代模型参数量不同（7B vs 8B），严格说不是受控对比；但差异幅度（31.9pp）远超参数量能解释的范围

## 11. 已知限制

- 仅测试了 Qwen3-VL-8B-Instruct，未对比其他模型
- 单一 prompt 模板，未做 prompt 敏感性分析
- greedy 解码，未测试采样方差
- 多图题未做「图像顺序打乱」的鲁棒性检验
- `L2_Q1.tsv` 中 1,032 行 `answer` 为空，已剔除；`L3_Q4.tsv` 字段结构不同，未纳入
